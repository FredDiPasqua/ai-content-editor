"use client";

import React from 'react';

const CopilotKitWrapper = ({ children }) => {
  return (
    <div className="copilot-wrapper">
      {/* Mocking CopilotKit */}
      {children}
    </div>
  );
};

export default CopilotKitWrapper;
